
{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{parent()}}
{% endblock %}

{% block body %}
    
    <h5>LOGIN</h5>
    
    <form class="form" action="{{ ('/logearse') }}">
        <label class="field" for="usuario" >Usuario: * </label>
        <input class="field" type="usuario" name="usuario" required >
        <label class="field" for="pass"> Contraseña: *</label>
        <input class="field" type="password" name="pass" required>

        <input class="mt-3" type="submit" value="Añadir">
    </form>
    <div class="tex-center">
        <p>¿No estás registrado?</p>
        <p> <a href="{{('/registro')}}">Registrate ahora</a></p>
    </div>
{% endblock %}

{% block javascripts %}
   
{% endblock %}


